/*
 * This file was generated automatically by ExtUtils::ParseXS version 2.2210 from the
 * contents of parallel_tree_builder_pl_bfe0.xs. Do not edit this file, edit parallel_tree_builder_pl_bfe0.xs instead.
 *
 *	ANY CHANGES MADE HERE WILL BE LOST! 
 *
 */

#line 1 "parallel_tree_builder_pl_bfe0.xs"
#include "EXTERN.h"
#include "perl.h"
#include "XSUB.h"
#include "INLINE.h"

void write_positions(char* refseq, char* seq, char* filename, char* filename2) {
	
	FILE* fh = fopen(filename, "w");
	FILE* fh2 = fopen(filename2, "w");
	int i;
	int g = 0; // gap
	int p = 0; // current position
	int s = 0; // start of alignment block
	int g2 = 0;
	int p2 = 0;
	int s = 0;
	
	// Alignment blocks are interupted by gaps
	// Gap columns are ignored
	// Alignment blocks are printed as
	// ref_start, seq_start, ref_end, seq_end, ref_gap_offset, seq_gap_offset
		
	if (fh == NULL) {
		fprintf(stderr, "Can't open output file %s!\n",
			filename);
		exit(1);
	}
	
	if (fh2 == NULL) {
		fprintf(stderr, "Can't open output file %s!\n",
			filename2);
		exit(1);
	}
	                                         
	for(i=0; refseq[i] && seq[i]; ++i) {
		
		// Advance reference sequence counters
		if(refseq[i] == '-') {
			
			if(seq[i] != '-') {
				// End of alignment block
				fprintf(fh2, "%i\t%i\t%i\t%i\t%i\t%i\n", s, s2, p, p2, g, g2);
			}
			
			g++;
			s = p;
			
		} else {
			
			if(g != 0) {
				// End of alignment block
				fprintf(fh2, "%i\t%i\t%i\t%i\t%i\t%i\n", s, s2, p, p2, g, g2);
				
				g = 0;
				s = p;
			}
			
			p++;
		}
		
		// Advance comparison sequence counters
		if(seq[i] == '-') {
			
			// End of alignment block
			fprintf(fh2, "%i\t%i\t%i\t%i\t%i\t%i\n", s, s2, p, p2, g, g2);
			
			g2++;
			s2 = p2;
			
	
		} else {
			
			if(g2 != 0) {
				// End of alignment block
				fprintf(fh2, "%i\t%i\t%i\t%i\t%i\t%i\n", s, s2, p, p2, g, g2);
				
				g2 = 0;
				s2 = p2;
			}
			
			p2++;
		}
		
		// Print SNP                                        
		if(refseq[i] != seq[i]) {
			fprintf(fh, "%i\t%i\t%c\t%c\n", p, g, refseq[i], seq[i]);
		}
		
		                                                                       
	}
	
	fclose(fh);                                                                           

}

void snp_positions(SV* seqs_arrayref, SV* names_arrayref, char* refseq, char* fileroot) {
	
	AV* names;
	AV* seqs;
	
	names = (AV*)SvRV(names_arrayref);
	seqs = (AV*)SvRV(seqs_arrayref);
	int n = av_len(seqs);
	int i;
	
	// compare each seq to ref
	// write snps to file for genome
	for(i=0; i <= n; ++i) {
		SV* name = av_shift(names);
		SV* seq = av_shift(seqs);
		char filename[120];
		char filename2[120];
		sprintf(filename, "%s__%s__snp_variations.txt", fileroot, (char*)SvPV_nolen(name));
		sprintf(filename2, "%s__%s__snp_positions.txt", fileroot, (char*)SvPV_nolen(name));
		
		write_positions(refseq, (char*)SvPV_nolen(seq), filename, filename2);
		
	}
	
}







#line 138 "parallel_tree_builder_pl_bfe0.c"
#ifndef PERL_UNUSED_VAR
#  define PERL_UNUSED_VAR(var) if (0) var = var
#endif

#ifndef PERL_ARGS_ASSERT_CROAK_XS_USAGE
#define PERL_ARGS_ASSERT_CROAK_XS_USAGE assert(cv); assert(params)

/* prototype to pass -Wmissing-prototypes */
STATIC void
S_croak_xs_usage(pTHX_ const CV *const cv, const char *const params);

STATIC void
S_croak_xs_usage(pTHX_ const CV *const cv, const char *const params)
{
    const GV *const gv = CvGV(cv);

    PERL_ARGS_ASSERT_CROAK_XS_USAGE;

    if (gv) {
        const char *const gvname = GvNAME(gv);
        const HV *const stash = GvSTASH(gv);
        const char *const hvname = stash ? HvNAME(stash) : NULL;

        if (hvname)
            Perl_croak(aTHX_ "Usage: %s::%s(%s)", hvname, gvname, params);
        else
            Perl_croak(aTHX_ "Usage: %s(%s)", gvname, params);
    } else {
        /* Pants. I don't think that it should be possible to get here. */
        Perl_croak(aTHX_ "Usage: CODE(0x%"UVxf")(%s)", PTR2UV(cv), params);
    }
}
#undef  PERL_ARGS_ASSERT_CROAK_XS_USAGE

#ifdef PERL_IMPLICIT_CONTEXT
#define croak_xs_usage(a,b)	S_croak_xs_usage(aTHX_ a,b)
#else
#define croak_xs_usage		S_croak_xs_usage
#endif

#endif

/* NOTE: the prototype of newXSproto() is different in versions of perls,
 * so we define a portable version of newXSproto()
 */
#ifdef newXS_flags
#define newXSproto_portable(name, c_impl, file, proto) newXS_flags(name, c_impl, file, proto, 0)
#else
#define newXSproto_portable(name, c_impl, file, proto) (PL_Sv=(SV*)newXS(name, c_impl, file), sv_setpv(PL_Sv, proto), (CV*)PL_Sv)
#endif /* !defined(newXS_flags) */

#line 190 "parallel_tree_builder_pl_bfe0.c"

XS(XS_main_write_positions); /* prototype to pass -Wmissing-prototypes */
XS(XS_main_write_positions)
{
#ifdef dVAR
    dVAR; dXSARGS;
#else
    dXSARGS;
#endif
    if (items != 4)
       croak_xs_usage(cv,  "refseq, seq, filename, filename2");
    PERL_UNUSED_VAR(ax); /* -Wall */
    SP -= items;
    {
	char *	refseq = (char *)SvPV_nolen(ST(0));
	char *	seq = (char *)SvPV_nolen(ST(1));
	char *	filename = (char *)SvPV_nolen(ST(2));
	char *	filename2 = (char *)SvPV_nolen(ST(3));
#line 140 "parallel_tree_builder_pl_bfe0.xs"
	I32* temp;
#line 211 "parallel_tree_builder_pl_bfe0.c"
#line 142 "parallel_tree_builder_pl_bfe0.xs"
	temp = PL_markstack_ptr++;
	write_positions(refseq, seq, filename, filename2);
	if (PL_markstack_ptr != temp) {
          /* truly void, because dXSARGS not invoked */
	  PL_markstack_ptr = temp;
	  XSRETURN_EMPTY; /* return empty stack */
        }
        /* must have used dXSARGS; list context implied */
	return; /* assume stack size is correct */
#line 222 "parallel_tree_builder_pl_bfe0.c"
	PUTBACK;
	return;
    }
}


XS(XS_main_snp_positions); /* prototype to pass -Wmissing-prototypes */
XS(XS_main_snp_positions)
{
#ifdef dVAR
    dVAR; dXSARGS;
#else
    dXSARGS;
#endif
    if (items != 4)
       croak_xs_usage(cv,  "seqs_arrayref, names_arrayref, refseq, fileroot");
    PERL_UNUSED_VAR(ax); /* -Wall */
    SP -= items;
    {
	SV *	seqs_arrayref = ST(0);
	SV *	names_arrayref = ST(1);
	char *	refseq = (char *)SvPV_nolen(ST(2));
	char *	fileroot = (char *)SvPV_nolen(ST(3));
#line 159 "parallel_tree_builder_pl_bfe0.xs"
	I32* temp;
#line 248 "parallel_tree_builder_pl_bfe0.c"
#line 161 "parallel_tree_builder_pl_bfe0.xs"
	temp = PL_markstack_ptr++;
	snp_positions(seqs_arrayref, names_arrayref, refseq, fileroot);
	if (PL_markstack_ptr != temp) {
          /* truly void, because dXSARGS not invoked */
	  PL_markstack_ptr = temp;
	  XSRETURN_EMPTY; /* return empty stack */
        }
        /* must have used dXSARGS; list context implied */
	return; /* assume stack size is correct */
#line 259 "parallel_tree_builder_pl_bfe0.c"
	PUTBACK;
	return;
    }
}

#ifdef __cplusplus
extern "C"
#endif
XS(boot_parallel_tree_builder_pl_bfe0); /* prototype to pass -Wmissing-prototypes */
XS(boot_parallel_tree_builder_pl_bfe0)
{
#ifdef dVAR
    dVAR; dXSARGS;
#else
    dXSARGS;
#endif
#if (PERL_REVISION == 5 && PERL_VERSION < 9)
    char* file = __FILE__;
#else
    const char* file = __FILE__;
#endif

    PERL_UNUSED_VAR(cv); /* -W */
    PERL_UNUSED_VAR(items); /* -W */
#ifdef XS_APIVERSION_BOOTCHECK
    XS_APIVERSION_BOOTCHECK;
#endif
    XS_VERSION_BOOTCHECK ;

        newXS("main::write_positions", XS_main_write_positions, file);
        newXS("main::snp_positions", XS_main_snp_positions, file);
#if (PERL_REVISION == 5 && PERL_VERSION >= 9)
  if (PL_unitcheckav)
       call_list(PL_scopestack_ix, PL_unitcheckav);
#endif
    XSRETURN_YES;
}

